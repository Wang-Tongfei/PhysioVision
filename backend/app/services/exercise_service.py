"""Exercise service (stub)."""
from app.services.base import BaseService


class ExerciseService(BaseService):
    """Business logic for rehabilitation exercises."""

    # TODO: async def list(self, db, category, skip, limit) -> list[Exercise]
    # TODO: async def create(self, db, payload) -> Exercise
    # TODO: async def get(self, db, exercise_id) -> Exercise | None
